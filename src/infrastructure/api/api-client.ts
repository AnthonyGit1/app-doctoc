// Cliente de API para interactuar con las APIs de Doctoc
// Este archivo será implementado cuando tengamos las especificaciones de la API
export class DoctocApiClient {
  private baseUrl: string;
  
  constructor(baseUrl: string) {
    this.baseUrl = baseUrl;
  }

  // Métodos para interactuar con las APIs de Doctoc serán implementados aquí
}